#control_panel
  =link_to 'Sync Commit List', sync_commits_path,        :class => 'button'
  =link_to 'Reset',erase_commits_path, :class => 'button', :confirm => 'Are you sure? This will erase all commit review history.' #unless ((@new_commits.size + @rejected_commits.size) < 1)
